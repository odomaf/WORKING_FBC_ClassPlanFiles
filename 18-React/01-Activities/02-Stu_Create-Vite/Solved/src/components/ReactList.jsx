// This component returns a list of items.
// It demonstrates how JSX can return multiple HTML elements inside one parent element.
function ReactList() {
  return (
    <ul>
      <li>Components organize the UI</li>
      <li>JSX blends JavaScript and HTML</li>
      <li>Data can be displayed using curly braces</li>
      <li>Applications start from a root component</li>
    </ul>
  );
}

// Exporting the component so it can be imported elsewhere.
export default ReactList;
