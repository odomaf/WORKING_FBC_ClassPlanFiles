const mongoose = require("mongoose");

/**
 * TODO:
 * 1. Define bookSchema with:
 *    - title (required string)
 *    - author (string)
 *    - price (required number)
 */

/**
 * TODO:
 * Add an instance method named getDiscount
 * - Reduce the price by 50%
 * - Console log the book title
 * - Console log the discounted price
 */

// bookSchema.methods.getDiscount = function () {
//   ...
// };

// TODO: Call the instance method here

// discountedBook.getDiscount();

module.exports = Book;
