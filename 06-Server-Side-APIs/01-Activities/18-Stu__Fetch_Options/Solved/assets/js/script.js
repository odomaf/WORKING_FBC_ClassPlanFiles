// TODO: Make a fetch() request to the NASA APOD API.
// Use the APOD base URL and add at least the api_key parameter.

// Example:
// const requestUrl = "https://api.nasa.gov/planetary/apod?api_key=DEMO_KEY";

// TODO: Call fetch(requestUrl, { cache: 'reload' })

// TODO: In the first .then(), return response.json()

// TODO: In the next .then(), console.log the data to inspect the response

// Optional: Add a .catch() to log any errors
