// 02-Stu — jQuery Elements (Unsolved)
// Goal: Use jQuery to create a quote <h1> and an author <p>, then append to #root.
// Steps to implement (remove TODO markers as you finish):
//
// 1) Select the existing DOM element <main id="root"> and assign it to a variable named rootEl
//    const rootEl = $('#root');
//
// 2) Create a <p> element for the author and assign it to a variable named authorEl
//    const authorEl = $('<p>');
//
// 3) Set the text content of authorEl to "~ Carol Dweck"
//    authorEl.text('~ Carol Dweck');
//
// 4) Add the class "plain" to authorEl
//    authorEl.addClass('plain');
//
// 5) Create an <h1> element for the quote and assign it to a variable named quoteEl
//    const quoteEl = $('<h1>');
//
// 6) Set the text content of quoteEl to:
//    "Love Challenges, Be Intrigued by Mistakes, Enjoy Effort, and Keep Learning."
//
// 7) Add the class "fancy" to quoteEl
//    Prefer: quoteEl.addClass('fancy');
//
// 8) Append the authorEl INSIDE the quoteEl
//
// 9) Append the quoteEl into rootEl
//
// 10) (Optional) Log counts to verify:
//     console.log('H1 count:', rootEl.find('h1').length);
//     console.log('Author inside H1:', rootEl.find('h1 > p').length === 1);

$(function () {
    // TODO: 1) Select #root
    // const rootEl = ...

    // TODO: 2) Create <p> for the author

    // TODO: 3) Set text "~ Carol Dweck"

    // TODO: 4) Add class "plain"

    // TODO: 5) Create <h1> for the quote

    // TODO: 6) Set quote text:
    // "Love Challenges, Be Intrigued by Mistakes, Enjoy Effort, and Keep Learning."

    // TODO: 7) Add class "fancy"

    // TODO: 8) Nest the author <p> inside the <h1>

    // TODO: 9) Append the <h1> to #root

    // (Optional checks)
    // console.log('H1 count:', rootEl.find('h1').length);
    // console.log('Author inside H1:', rootEl.find('h1 > p').length === 1);
});
