# 🧱 14-Stu_Bootstrap-Layout

## 📖 Your Task

Work with a partner to implement the following user story:

> **As a developer**, I want to create a **responsive webpage layout** using the Bootstrap grid system — **without writing any custom CSS**.

---

## ✅ Acceptance Criteria

- It's done when the page displays **multiple columns on large screens** and collapses into a **single-column layout on smaller screens**.
- It's done when the layout matches the provided mock-up as closely as possible using **only Bootstrap classes**.
- It's done when images and text resize fluidly using `.img-fluid`, `.container`, `.row`, and `.col-*`.

---

## 🧭 Getting Started

1. Open `index.html` in your browser.
2. Review how the page looks at different screen widths.
3. Add or adjust Bootstrap grid classes such as:
   - `.container` / `.container-fluid`
   - `.row`
   - `.col-12`, `.col-md-6`, `.col-lg-3`
   - Alignment utilities like `.justify-content-center`, `.align-items-e
